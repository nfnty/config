# set -s buffer-limit 20
# set -s default-terminal "screen"
set -s escape-time 0
set -s exit-unattached off
set -s focus-events off
# set -s message-limit 100
set -s set-clipboard off
# set -s terminal-overrides "*256col*:colors=256"
#                           ",xterm*:XT:Ms=\\E]52;%p1%s;%p2%s\\007"
#                           ":Cs=\\E]12;%p1%s\\007:Cr=\\E]112\\007"
#                           ":Ss=\\E[%p1%d q:Se=\\E[2 q,screen*:XT"

# set -g assume-paste-time 1
# set -g base-index 0
# set -g bell-action any
# set -g bell-on-alert off
# set -g default-command ""
# set -g default-shell $SHELL
# set -g destroy-unattached off
set -g detach-on-destroy off
# set -g display-panes-active-colour 1
# set -g display-panes-colour 4
# set -g display-panes-time 1000
# set -g display-time 750
set -g history-limit 500000
# set -g lock-after-time 0
# set -g lock-command "lock -np"
# set -g lock-server on
set -g message-command-style "bg=colour233,fg=colour178"
set -g message-style "bg=black,fg=colour178"
# set -g mouse off
set -g mouse-resize-pane off
set -g mouse-select-pane off
set -g mouse-select-window off
# set -g mouse-utf8 off
set -g prefix C-o
# set -g prefix2 NULL
# set -g renumber-windows off
set -g repeat-time 0
set -g set-remain-on-exit on
set -g set-titles on
set -g set-titles-string "#{session_name}:#{window_index}:#{window_name} - #{pane_title} #{session_alerts}"
# set -g status on
set -g status-interval 1
set -g status-justify centre
set -g status-keys vi
set -g status-left "[#{session_name}] #{pane_title}"
set -g status-left-length 30
# set -g status-left-style "default"
# set -g status-position bottom
set -g status-right "#{history_bytes}"
set -g status-right-length 30
# set -g status-right-style "default"
set -g status-style "bg=black,fg=colour167"
# set -g status-utf8 off
# set -g update-environment "DISPLAY SSH_ASKPASS SSH_AUTH_SOCK SSH_AGENT_PID SSH_CONNECTION WINDOWID XAUTHORITY"
# set -g visual-activity off
set -g visual-bell on
# set -g visual-silence on
# set -g word-separators " -_@"

setw -g aggressive-resize on
# setw -g allow-rename on
# setw -g alternate-screen on
# setw -g automatic-rename on
setw -g automatic-rename-format "#{pane_current_command}#{?pane_in_mode,[m],}#{?pane_dead,[d],}"
# setw -g c0-change-interval 100
# setw -g c0-change-trigger 250
# setw -g clock-mode-colour 4
# setw -g clock-mode-style 24
# setw -g force-height 0
# setw -g force-width 0
# setw -g main-pane-height 24
# setw -g main-pane-width 80
setw -g mode-keys vi
# setw -g mode-mouse off
setw -g mode-style "bg=black,fg=colour178"
# setw -g monitor-activity off
# setw -g monitor-silence 0
# setw -g other-pane-height 0
# setw -g other-pane-width 0
# setw -g pane-active-border-style "fg=green"
# setw -g pane-base-index 0
# setw -g pane-border-style "default"
# setw -g remain-on-exit off
# setw -g synchronize-panes off
# setw -g utf8 off
# setw -g window-active-style "default"
# setw -g window-style "default"
# setw -g window-status-activity-style "reverse"
# setw -g window-status-bell-style "reverse"
setw -g window-status-current-format "#{window_index}:#{window_name}#{?window_flags,#{window_flags},}"
# setw -g window-status-current-style "default"
setw -g window-status-format "#{window_index}:#{window_name}#{?window_flags,#{window_flags},}"
# setw -g window-status-last-style "default"
setw -g window-status-separator " "
# setw -g window-status-style "default"
# setw -g xterm-keys off
# setw -g wrap-search on

unbind-key -a
unbind-key -a -t vi-edit
unbind-key -a -ct vi-edit
unbind-key -a -t vi-copy
unbind-key -a -t vi-choice

bind C-o send-prefix
bind 0 select-window -t 0
bind 1 select-window -t 1
bind 2 select-window -t 2
bind 3 select-window -t 3
bind 4 select-window -t 4
bind 5 select-window -t 5
bind 6 select-window -t 6
bind 7 select-window -t 7
bind 8 select-window -t 8
bind 9 select-window -t 9
bind : command-prompt
bind b choose-buffer
bind B list-buffers
bind l last-window
bind L switch-client -l
bind n new-window
bind N new-session
bind y copy-mode
bind p paste-buffer
bind d detach-client
bind r respawn-window
bind R refresh-client
bind t choose-tree
bind w choose-window
bind m show-messages
bind M display-message
bind s set status
bind q kill-window

bind-key -t  vi-edit    C-c cancel
bind-key -t  vi-edit    Tab complete
bind-key -t  vi-edit  Enter enter
bind-key -t  vi-edit    C-u delete-line
bind-key -t  vi-edit    C-w delete-word
bind-key -t  vi-edit Escape switch-mode
bind-key -t  vi-edit BSpace backspace
bind-key -t  vi-edit    C-a start-of-line
bind-key -t  vi-edit    C-e end-of-line
bind-key -t  vi-edit     Up history-up
bind-key -t  vi-edit   Down history-down
bind-key -t  vi-edit   Left cursor-left
bind-key -t  vi-edit  Right cursor-right

bind-key -ct vi-edit Escape cancel
bind-key -ct vi-edit  Enter enter
bind-key -ct vi-edit    '$' end-of-line
bind-key -ct vi-edit      0 start-of-line
bind-key -ct vi-edit      A switch-mode-append-line
bind-key -ct vi-edit      B previous-space
bind-key -ct vi-edit      C switch-mode-change-line
bind-key -ct vi-edit      D delete-end-of-line
bind-key -ct vi-edit      J next-space-end
bind-key -ct vi-edit      I switch-mode-begin-line
bind-key -ct vi-edit      S switch-mode-substitute-line
bind-key -ct vi-edit      W next-space
bind-key -ct vi-edit      ^ start-of-line
bind-key -ct vi-edit      a switch-mode-append
bind-key -ct vi-edit      b previous-word
bind-key -ct vi-edit      d delete-line
bind-key -ct vi-edit      j next-word-end
bind-key -ct vi-edit      h cursor-left
bind-key -ct vi-edit      i switch-mode
bind-key -ct vi-edit      e history-down
bind-key -ct vi-edit      u history-up
bind-key -ct vi-edit      t cursor-right
bind-key -ct vi-edit      p paste
bind-key -ct vi-edit      s switch-mode-substitute
bind-key -ct vi-edit      w next-word
bind-key -ct vi-edit      l delete
bind-key -ct vi-edit BSpace backspace
bind-key -ct vi-edit     Up history-up
bind-key -ct vi-edit   Down history-down
bind-key -ct vi-edit   Left cursor-left
bind-key -ct vi-edit  Right cursor-right

bind-key -t vi-choice     U page-up
bind-key -t vi-choice     E page-down
bind-key -t vi-choice Escape cancel
bind-key -t vi-choice   Enter choose
bind-key -t vi-choice   Space tree-toggle
bind-key -t vi-choice       G end-of-list
bind-key -t vi-choice       H top-line
bind-key -t vi-choice       L bottom-line
bind-key -t vi-choice       g start-of-list
bind-key -t vi-choice       e down
bind-key -t vi-choice       u up
bind-key -t vi-choice       q cancel
bind-key -t vi-choice  BSpace backspace
bind-key -t vi-choice      Up up
bind-key -t vi-choice    Down down
bind-key -t vi-choice       h tree-collapse
bind-key -t vi-choice       t tree-expand
bind-key -t vi-choice    Left tree-collapse
bind-key -t vi-choice   Right tree-expand
bind-key -t vi-choice       1 start-number-prefix
bind-key -t vi-choice       2 start-number-prefix
bind-key -t vi-choice       3 start-number-prefix
bind-key -t vi-choice       4 start-number-prefix
bind-key -t vi-choice       5 start-number-prefix
bind-key -t vi-choice       6 start-number-prefix
bind-key -t vi-choice       7 start-number-prefix
bind-key -t vi-choice       8 start-number-prefix
bind-key -t vi-choice       9 start-number-prefix

bind-key -t vi-copy        E halfpage-down
bind-key -t vi-copy        y copy-selection
bind-key -t vi-copy        U halfpage-up
bind-key -t vi-copy Escape clear-selection
bind-key -t vi-copy      v begin-selection
bind-key -t vi-copy    '"' start-named-buffer
bind-key -t vi-copy    '$' end-of-line
bind-key -t vi-copy      , jump-reverse
bind-key -t vi-copy      / search-forward
bind-key -t vi-copy      0 start-of-line
bind-key -t vi-copy      1 start-number-prefix
bind-key -t vi-copy      2 start-number-prefix
bind-key -t vi-copy      3 start-number-prefix
bind-key -t vi-copy      4 start-number-prefix
bind-key -t vi-copy      5 start-number-prefix
bind-key -t vi-copy      6 start-number-prefix
bind-key -t vi-copy      7 start-number-prefix
bind-key -t vi-copy      8 start-number-prefix
bind-key -t vi-copy      9 start-number-prefix
bind-key -t vi-copy      : goto-line
bind-key -t vi-copy     \; jump-again
bind-key -t vi-copy      ? search-backward
bind-key -t vi-copy      A append-selection
bind-key -t vi-copy      B previous-space
bind-key -t vi-copy      D copy-end-of-line
bind-key -t vi-copy      J next-space-end
bind-key -t vi-copy      F jump-backward
bind-key -t vi-copy      G history-bottom
bind-key -t vi-copy      H top-line
bind-key -t vi-copy      L bottom-line
bind-key -t vi-copy      M middle-line
bind-key -t vi-copy      N search-reverse
bind-key -t vi-copy      V select-line
bind-key -t vi-copy      W next-space
bind-key -t vi-copy      ^ back-to-indentation
bind-key -t vi-copy      b previous-word
bind-key -t vi-copy      j next-word-end
bind-key -t vi-copy      f jump-forward
bind-key -t vi-copy      g history-top
bind-key -t vi-copy      h cursor-left
bind-key -t vi-copy      e cursor-down
bind-key -t vi-copy      u cursor-up
bind-key -t vi-copy      t cursor-right
bind-key -t vi-copy      n search-again
bind-key -t vi-copy      o other-end
bind-key -t vi-copy      q cancel
bind-key -t vi-copy      T jump-to-forward
bind-key -t vi-copy    C-v rectangle-toggle
bind-key -t vi-copy      w next-word
